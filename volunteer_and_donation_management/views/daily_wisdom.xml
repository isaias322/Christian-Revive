<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- List View -->
    <record id="view_daily_wisdom_list" model="ir.ui.view">
        <field name="name">daily.wisdom.list</field>
        <field name="model">daily.wisdom</field>
        <field name="arch" type="xml">
            <list string="Daily Wisdoms">
                <field name="date"/>
                <field name="name"/>
                <field name="is_published"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_daily_wisdom_form" model="ir.ui.view">
        <field name="name">daily.wisdom.form</field>
        <field name="model">daily.wisdom</field>
        <field name="arch" type="xml">
            <form string="Daily Wisdom">
                <header>
                    <button name="action_publish" type="object"
                            string="Publish" class="btn-primary"
                            invisible="is_published"/>
                    <button name="action_unpublish" type="object"
                            string="Unpublish" class="btn-secondary"
                            invisible="not is_published"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Wisdom Title..."/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="date"/>
                            <field name="is_published"/>
                            <field name="day_number"/>
                        </group>
                        <group>
                            <field name="image" widget="image" class="oe_avatar"/>
                            <field name="video" filename="video_filename"/>
                            <field name="video_url"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Content" name="content">
                            <field name="content" widget="html" placeholder="Write your wisdom content here..."/>
                        </page>
                        <page string="Summary" name="summary">
                            <field name="summary" placeholder="Brief summary..."/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_daily_wisdom_search" model="ir.ui.view">
        <field name="name">daily.wisdom.search</field>
        <field name="model">daily.wisdom</field>
        <field name="arch" type="xml">
            <search string="Search Wisdoms">
                <field name="name"/>
                <filter name="published" string="Published"
                        domain="[('is_published','=',True)]"/>
                <filter name="unpublished" string="Unpublished"
                        domain="[('is_published','=',False)]"/>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_daily_wisdom" model="ir.actions.act_window">
        <field name="name">Daily Wisdoms</field>
        <field name="res_model">daily.wisdom</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_daily_wisdom_search"/>
    </record>

</odoo>