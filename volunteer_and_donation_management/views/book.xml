<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_book_form" model="ir.ui.view">
        <field name="name">library.book.form</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <form>
                <!-- ── NEW: Extract button ── -->
                <header>
                    <button name="action_extract_pdf_text"
                            string="Extract PDF Text"
                            type="object"
                            class="btn-primary"/>
                </header>

                <sheet>
                    <field name="image" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Book Title"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="author"/>
                            <field name="date"/>
                            <field name="category"/>
                            <field name="is_published"/>
                        </group>
                        <group>
                            <field name="pdf_file" filename="pdf_filename"/>
                            <field name="pdf_filename" invisible="1"/>
                            <field name="language"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                        <page string="Content">
                            <field name="content"/>
                        </page>
                        <!-- ── NEW: PDF Text tab ── -->
                        <page string="PDF Text">
                            <field name="pdf_text"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_book_list" model="ir.ui.view">
        <field name="name">library.book.list</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="author"/>
                <field name="date"/>
                <field name="category"/>
                <field name="language"/>
                <field name="is_published"/>
            </list>
        </field>
    </record>

    <record id="action_books" model="ir.actions.act_window">
        <field name="name">Books</field>
        <field name="res_model">library.book</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>